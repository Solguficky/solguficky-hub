# Журнал наблюдений

> **Статус:** Current. Журнал хранит повторяющиеся сигналы о процессе и коде до того, как они станут действующим правилом.

Наблюдение фиксирует проверяемый факт, который пока встретился недостаточно часто для общего правила. Это не backlog, не решение и не место для обязательных требований: работу ведём в Linear, решения — в ADR, действующие правила — в `docs/standards/` и инструкциях соответствующей области. Это и не общий список открытых вопросов, которого в `docs/` намеренно нет: журнал хранит случившиеся факты, а вопрос живёт там, где его закрывают — по правилам [docs/README.md](../README.md).

## Когда добавлять запись

Добавляй наблюдение после конкретного случая, если оно может повториться и повлиять на способ дальнейшей работы. Не записывай сюда разовые предпочтения, уже принятые решения и требования отдельной задачи.

## Форма записи

```markdown
### YYYY-MM-DD — Короткое название

- **Контекст:** задача, PR, команда или файл, в котором появился сигнал.
- **Наблюдение:** что произошло, без преждевременного общего правила.
- **Последствие:** чем это помешало продукту, разработке или проверке.
- **Адресат:** предполагаемое место будущего правила из таблицы ниже.
- **Повторение:** первое наблюдение либо ссылка на независимый предыдущий случай.
- **Следующий сигнал:** по какому проверяемому событию считать наблюдение повторившимся.
- **Статус:** наблюдение | вынесено на обсуждение | переведено в правило.
```

## Адресаты

Адресат — место, в котором наблюдение станет действующим правилом, если повторится. Он называется сразу, вместе с записью: наблюдение без адресата нельзя ни проверить, ни перевести, и оно оседает в журнале как общее пожелание.

| Адресат | Когда он верный | Что получает правило |
|---|---|---|
| Автоматическая проверка | сигнал проверяется командой и не требует суждения | рецепт в `justfile`, тест или скрипт в `tools/` |
| Хук | проверка уже существует, но запускается не в тот момент или не запускается вовсе | `.claude/settings.json` |
| Скилл | сигнал про последовательность работы: шаг пропущен, повторён или сделан не в том порядке | `.skillshare/skills/` |
| Норматив | правило требует суждения, действует на весь репозиторий и проверяется на review | `docs/standards/` |
| Правило в `AGENTS.md` | поведение агента меняется до того, как он откроет хоть один файл: навигация, граница автономии, критическое ограничение | корневой или вложенный `AGENTS.md` |

Адресат выбирается сверху вниз, и ниже спускаются только когда верхний не подходит. Порядок отражает цену: выполненная проверка ничего не стоит на каждом следующем прогоне, а строка в `AGENTS.md` занимает контекст любой сессии независимо от того, относится она к делу или нет. Правило, которое можно выполнить командой, не формулируется словами.

Один сигнал может иметь два адресата — проверку и хук, который её запускает. Тогда оба называются в записи; лишним адресатом наблюдение не портится, а пропущенным становится невыполнимым.

## Когда переводить в правило

После второго независимого подтверждения одного и того же сигнала наблюдение выносится на обсуждение. Независимый случай — из другой задачи или другой сессии: один инцидент, записанный дважды, сам себя не подтверждает. Перевод допустим, если из случаев получается повторяемое и проверяемое правило с понятной областью применимости. Само повторение не вводит правило автоматически: владелец явно принимает формулировку, после чего она переносится к адресату записи, а сама запись получает ссылку на новый источник правды.

## Наблюдения

### 2026-08-30 — Отклонение за границами задачи не дошло до затронутой задачи Linear

- **Контекст:** PER-84, PER-88, [PR #28](https://github.com/Solguficky/solguficky-hub/pull/28), коммит `fc494b8`.
- **Наблюдение:** PER-84 удалила скилл `proj-delegate-subtask` и агента `subtask-impl`. Ни того, ни другого не было в её разделе «Что сделать» и в критериях приёмки. Отклонение записано в тело PR по [шагу отчёта в контуре](agent-execution-loop.md) и больше никуда. PER-88 просит перенести ветвление того же скилла на gitflow, то есть описывает вариант, отклонённый в PER-84; она осталась в Backlog нетронутой и на следующий день была переведена в In Progress.
- **Последствие:** задача взята в работу, заведена ветка. Её несостоятельность обнаружилась только чтением git-истории и тела чужого PR, уже внутри сессии. Тело PR читают в момент ревью, а автор следующей задачи открывает Linear.
- **Адресат:** Скилл — `proj-deliver-task`. Верхние адресаты не подходят: связь «удалённый файл — чужая задача» требует суждения и знания трекера, командой не проверяется, и проверки, которую запускал бы хук, не существует. Сигнал про пропущенный шаг отчёта, а не про правило репозитория.
- **Повторение:** первое наблюдение.
- **Следующий сигнал:** задача, взятая в работу, чей предмет уже удалён или переписан смерженной соседней задачей.
- **Статус:** наблюдение

### 2026-09-02 — Ревью не проверило состояния, созданные самим изменением

- **Контекст:** PER-96, [PR #48](https://github.com/Solguficky/solguficky-hub/pull/48), коммиты `510131a` и `7d09dc1`.
- **Наблюдение:** `proj-review-change` прошёл изменение тремя осями и находок такого рода не дал. После открытия PR внешние ревьюеры нашли три дефекта, все — в состояниях, которых до этого изменения не существовало. Изменение вводило draft PR как носитель чекпоинта, то есть состояние «на ветке уже есть PR, но работа не сдана»; шаг сдачи после него упирался в `a pull request for branch ... already exists`, а запрет открывать PR на красном гейте смыкался с требованием фиксировать остановку и терял работу фонового запуска. Третья находка того же рода: признак stale-дерева «нет ни коммитов, ни правок» выполняется сразу после `git worktree add`.
- **Последствие:** два коммита-фикса уже после сдачи. Ось корректности при этом сработала — она нашла соседний дефект в том же месте, — но искала недостающее, а не последствия добавленного.
- **Адресат:** Скилл — `proj-review-change`. Верхние адресаты не подходят: связь «новая сущность порождает новые переходы» требует суждения, командой не проверяется, и проверки, которую запускал бы хук, не существует.
- **Повторение:** первое наблюдение.
- **Следующий сигнал:** находка внешнего ревью на PR в состоянии, которое ввело само это изменение, при пройденных трёх осях.
- **Статус:** наблюдение

### 2026-09-02 — Зелёный `just verify` при полностью пропущенных тестах

- **Контекст:** PER-96, `just verify`, `apps/identity/internal/migrations/apply_test.go`.
- **Наблюдение:** без `IDENTITY_DATABASE_URL` тесты схемы Identity пропускаются. `go test -v ./internal/migrations/` даёт шесть `--- SKIP`, при этом `go test` печатает `ok`, а `just verify` завершается зелёным. По выводу гейта пропуск неотличим от прохождения.
- **Последствие:** гейт даёт ложное покрытие. Факт обнаружен только чтением `apply_test.go` и отдельным прогоном с `-v`; оговорку пришлось писать руками в тело PR и в комментарий Linear.
- **Адресат:** Автоматическая проверка — `justfile`. Верхний адресат подходит: сигнал проверяется командой и суждения не требует.
- **Повторение:** второй независимый случай — PER-53, [PR #65](https://github.com/Solguficky/solguficky-hub/pull/65), другой сервис и другая сессия. Тесты схемы Meetups пропускаются, когда Testcontainers не находит Docker; на машине владельца пропускался 21 тест из 27, и весь новый физический слой не проверялся ни разу. Механика гейта подтверждена отдельно: временный тест с `Assert.Skip` даёт `just meetups-test` с кодом возврата 0 и сводкой «Пройден! успешно: 85, пропущено: 1». То есть пропуск неотличим от прохождения не по недосмотру читателя, а по устройству команды.
- **Следующий сигнал:** сигнал подтверждён дважды; дальше — формулировка правила, а не ожидание третьего случая.
- **Правило из двух случаев:** гейт различает «тест прошёл» и «тест пропущен». Годится порог: прогон, где пропущен хотя бы один тест, завершается ненулевым кодом, если пропуск не разрешён явно. Оба случая — про внешнюю зависимость (PostgreSQL, Docker), поэтому область применимости — тесты, которым нужна среда, а не весь набор.
- **Статус:** вынесено на обсуждение

### 2026-09-02 — Контур возобновлён на существующей ветке без сверки с `develop`

- **Контекст:** PER-96, ветка `feature/PER-96`, `git fetch origin`.
- **Наблюдение:** ветка задачи существовала до начала сессии, поэтому шаг создания ветки с `git fetch origin` не выполнялся, а шага сверки при возобновлении нет. Fetch был сделан позже и по другому поводу; он показал `ed87c41..373944f` — влилась PER-45, изменившая `AGENTS.md`, `justfile` и `.github/workflows/ci.yml`, то есть файлы, уже прочитанные в устаревшей версии.
- **Последствие:** часть сессии шла на устаревшем `AGENTS.md`; расхождение обнаружено случайно, а не проверкой. Команда сверки в [branching.md](../standards/git/branching.md) есть, но при возобновлении её никто не запускает.
- **Адресат:** Скилл — `proj-start-task`, шаг восстановления контекста. Автоматическая проверка не подходит: команда уже существует, вопрос в моменте запуска, а события инструмента, на которое повесить хук, у возобновления контура нет.
- **Повторение:** первое наблюдение.
- **Следующий сигнал:** возобновление работы на существующей `feature/PER-N`, пока `origin/develop` ушёл вперёд.
- **Статус:** наблюдение

### 2026-09-07 — Починка находки ревью включила в себя архитектурное решение

- **Контекст:** PER-53, [PR #65](https://github.com/Solguficky/solguficky-hub/pull/65), `apps/meetups/Meetups/Migrations/001_meetups_schema.sql`, [PER-164](https://linear.app/anticnvm/issue/per-164).
- **Наблюдение:** находка code review сказала, что колонка `position` (`GENERATED ALWAYS AS IDENTITY`) не может служить курсором outbox, и назвала два возможных лечения. На шаге «примени находки» агент выбрал одно из них — колонка `dispatched_at` с частичным индексом, — реализовал его, записал в бриф Meetups и подтвердил живым прогоном Aspire. Диагноз был верным и проверяемым; лечение было выбором из четырёх вариантов с разной ценой. Остановил это владелец вопросом о самой премиссе, после чего колонка снята, а выбор вынесен в отдельную задачу с меткой `Decision`.
- **Последствие:** схема написана, проверена и задокументирована, затем откачена; бриф переписан дважды. Ни один гейт на этом не срабатывает: `just verify` зелёный и до, и после отката, потому что оба состояния корректны. Правило «продуктовые и архитектурные решения принимает владелец» в `AGENTS.md` есть, но на шаге починки не сработало: находка ревью выглядит как баг, а не как развилка, и починка не читается как решение.
- **Адресат:** Скилл — `proj-review-change`. Оговорка: ревью шло встроенным `/code-review`, который в репозитории не правится, а своего скилла «примени находки» нет; `proj-review-change` — ближайшая собственная поверхность, и владельцу решать, туда ли уходит правило. Верхние адресаты не подходят: «лечение находки требует выбора, а не только диагноза» проверяется суждением, командой не ловится, и события для хука у него нет.
- **Повторение:** первое наблюдение.
- **Следующий сигнал:** находка ревью, чья починка меняет схему, межсервисный контракт или наблюдаемое поведение, применена без остановки на владельце.
- **Статус:** наблюдение

### 2026-09-12 — Номер ADR и RFC берётся при создании, а верен только перед мержем

- **Контекст:** PER-99, [PR #67](https://github.com/Solguficky/solguficky-hub/pull/67), мержи `75269a9` и `30c7c43`.
- **Наблюдение:** один документ трижды менял идентичность: `ADR-034` → `ADR-035` → `ADR-039` и `RFC-007` → `RFC-008` → `RFC-010`. Оба раза номер оказывался занят не в момент взятия, а в момент мержа: пока ветка жила, develop принял чужие документы с теми же номерами. Git коллизию не видит, потому что имена файлов разные и оба файла мержатся без конфликта; единственное место, где два документа встречаются в одной строке, — таблица индекса, и конфликтовала именно она. Обход remote-веток показывает ещё три неразрешённых дубля: `ADR-034` (`telegram-bot-rich-presentation` на develop против `single-netcup-vps` на `feature/PER-80`), `ADR-035` (`meetups-dispatch-mark-in-journal` на develop против `temporary-first-admin-bootstrap` на `feature/PER-163`), `RFC-007` (`auction-scope-and-format-options` на develop против `remote-development` на `feature/PER-80`).
- **Последствие:** два раунда переименований и двухпроходная правка ссылок, в которой чужие `RFC-008` и `ADR-035` с develop нужно было не задеть; ветка получила merge-коммит вместо чистой истории. Коллизия обнаружилась статусом `CONFLICTING` у pull request, то есть после сдачи. Проверки на номера нет ни в `tools/`, ни в `ci.yml`.
- **Адресат:** Автоматическая проверка — скрипт в `tools/` и джоба CI. Верхний адресат подходит: на событии `pull_request` CI видит merge ref, где оба файла лежат рядом, поэтому «номер встречается ровно один раз и у файла есть строка в индексе» проверяется командой и суждения не требует. Второй адресат — скилл `proj-record-decision`: его шаг «возьми следующий свободный номер» присваивает номер при создании документа.
- **Повторение:** второй независимый случай — 2026-09-07 и 2026-09-12, разные мержи и разные сессии. Третий, ещё не сработавший, лежит на `feature/PER-163`.
- **Следующий сигнал:** сигнал подтверждён дважды; дальше — формулировка правила, а не ожидание третьего случая.
- **Правило из двух случаев:** номер документа уникален в merge-результате, и у каждого файла есть строка в индексе своего каталога. Область применимости — `docs/decisions/` и `docs/rfcs/`; для номеров полей Protobuf то же правило уже действует в [protobuf.md](../standards/contracts/protobuf.md). Оба случая про долгоживущую ветку, поэтому проверка нужна на сдаче, а не при создании документа.
- **Статус:** вынесено на обсуждение

### 2026-09-12 — Рабочее дерево приезжает с таргетами, которых нет в Git, и гейт от этого красный

- **Контекст:** PER-99, дерево `.claude/worktrees/rfc-008-server-setup-review-497be3`, `just check-agent-tools`.
- **Наблюдение:** гейт упал на 52 строках вида «missing target `.agents/skills/<имя>`» при диффе из четырёх markdown-файлов. Причина не в правке и не в отсутствии источников: дерево, созданное приложением, получает `.claude/` целиком, вместе с 52 внешними таргетами, которых в Git нет, а `.agents/skills/` наполняется только из Git и содержит 15 каталогов `proj-*`. Отметки времени всех 67 каталогов в `.claude/skills/` совпадают с моментом создания дерева. `check-generated.sh` сверяет два таргета между собой, поэтому асимметрия читается как рассинхронизация. Контрольный опыт: дерево, созданное `git worktree add`, даёт 15 против 15, и `just check-agent-tools` в нём зелёный. `check-agent-tools` стоит первым в рецепте `verify`, поэтому остальные проверки не запускаются вовсе.
- **Последствие:** двенадцать рецептов прогнаны поодиночке, чтобы доказать нулевой код возврата, и в отчёте пришлось писать «красный по окружению» — то есть гейт не нёс информации о правке. Попытка привести дерево в рабочее состояние по документированному пути стоила отдельного отката, см. соседнюю запись.
- **Адресат:** Автоматическая проверка — `tools/skillshare/check-generated.sh`: он сверяет два таргета между собой, хотя источником правды объявлен `.skillshare/skills/`, и потому не отличает «таргет не разложен» от «таргет приехал мимо Git». Второй адресат — `AGENTS.md`, где путь «после клонирования или создания worktree» описан одинаково для обоих способов создания дерева, хотя начальное состояние у них разное.
- **Повторение:** первое наблюдение.
- **Следующий сигнал:** `just verify` красный на `check-agent-tools` в дереве, чей дифф не трогает `.skillshare/**`.
- **Статус:** наблюдение

### 2026-09-12 — Команды skillshare молча правят отслеживаемые файлы

- **Контекст:** PER-99, `skillshare install -p`, `.skillshare/config.yaml`, `.skillshare/skills/.metadata.json`.
- **Наблюдение:** `skillshare install -p` поставил 9 скиллов из 12 — `aspire-orchestration` заблокирован его собственным security audit с находкой уровня CRITICAL — и вычеркнул 2 строки из `.skillshare/config.yaml` и 19 из `.skillshare/skills/.metadata.json`. Оба файла отслеживаются. Отказ по одному скиллу в выводе был, а правка конфига — нет; она замечена только `git status --short`.
- **Последствие:** регрессия общего конфига едва не уехала в pull request с документацией, к которому отношения не имеет. Ни один гейт её не ловит: `check-agent-tools` читает таргеты и frontmatter, а не объявление зависимостей.
- **Адресат:** Автоматическая проверка — `tools/skillshare/`. Второй адресат — `AGENTS.md`, где `skillshare install -p` и `skillshare sync -p` описаны как безопасный путь после клонирования или создания дерева.
- **Повторение:** второй независимый случай — сессия «Check skillshare manifest hashes on Windows» от 2026-09-07 на ветке `feature/PER-140`, другое дерево и другая задача. Там `skillshare sync -p` после мержа переписал 62 строки в двух отслеживаемых манифестах. Причины разобраны экспериментом в изолированном проекте: хеши `managed` считаются по сырым байтам источника и расходятся между LF и CRLF на одном и том же содержимом, `mtimes` хранит наносекундные отметки файловой системы и различается между любыми двумя чекаутами, `updated_at` переписывается при каждом sync, включая холостой. Значения при этом не валидирует ни один гейт: подмена хеша на `deadbeef` дала побайтово идентичный вывод проверки, а добавленный позже `check_manifest_entries` намеренно спрашивает только наличие ключа. Пять записей из пятнадцати на develop не воспроизводятся ни из LF, ни из CRLF, то есть закоммиченный манифест уже разошёлся с собственными источниками. Команды разные, класс один: инструмент правит отслеживаемые файлы как собственное внутреннее состояние.
- **Следующий сигнал:** сигнал подтверждён дважды; дальше — формулировка правила, а не ожидание третьего случая.
- **Правило из двух случаев:** файл, который инструмент переписывает как локальный кеш и который не читает ни одна проверка, в Git не хранится — это про `.skillshare-manifest.json` в обоих таргетах. `config.yaml` и `.metadata.json` под правило не подпадают: они объявляют зависимости и остаются в Git, поэтому для них нужен обратный ход — правка после `install` должна быть заметной, а не молчаливой.
- **Статус:** вынесено на обсуждение

### 2026-09-12 — У вывода сессии вне контура нет носителя в репозитории

- **Контекст:** сессия «Check skillshare manifest hashes on Windows» от 2026-09-07, дерево `admiring-turing-86ccc8`, ветка `claude/upbeat-davinci-ef19c3`.
- **Наблюдение:** сессия подтвердила гипотезу про манифест экспериментом, доказала, что CI его значения не читает, и предложила починку из двух команд. Закончилась она предложением завести задачу или открыть контур. Пять дней спустя нет ни задачи, ни записи в журнале, ни правки; дерево стоит на коммите от 2026-08-29 с грязным рабочим состоянием от старого sync. В сессии PER-99 та же область снова стоила времени, а знание о вычёркивании конфига пришло из личной памяти агента, а не из репозитория.
- **Последствие:** результат исследования живёт только в транскрипте и находится поиском по сессиям. Правило фиксации остановки в [agent-execution-loop.md](agent-execution-loop.md) действует внутри контура, а вне его предписан показ `git status --short`; для вывода, который не меняет ни одного файла, носителя не описано.
- **Адресат:** Правило в `AGENTS.md`, раздел про границу контура. Верхние адресаты не подходят: «у исследования без правки файлов должен быть носитель» требует суждения о том, что считать выводом, командой не проверяется, и события для хука у конца сессии нет.
- **Повторение:** первое наблюдение.
- **Следующий сигнал:** вывод исследовательской сессии, найденный поиском по транскриптам, а не по документу, задаче или записи журнала.
- **Статус:** наблюдение
