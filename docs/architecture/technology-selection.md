# Выбор языков и технологий

> **Статус:** Canonical для процесса выбора. Technology pool не является распределением будущих сервисов по языкам.

Язык назначается существующей задаче, а не наоборот.

## Матрица решения

Для сервиса сначала фиксируются:

- доменная сложность и инварианты;
- владение данными;
- consistency и concurrency;
- latency и throughput;
- интеграции и persistence;
- observability;
- зрелость библиотек;
- deployment footprint;
- скорость разработки владельца;
- учебная отдача именно на этом домене;
- стоимость ещё одного operational stack.

Кандидаты сравниваются по одной шкале. Обязательные ограничения применяются до субъективного интереса к языку. Решение для конкретного сервиса фиксируется ADR после появления требований и последствий.

## Technology pool

| Технология | Текущая позиция |
|---|---|
| TypeScript + grammY | Accepted для Telegram Bot ([ADR-030](../decisions/ADR-030-telegram-bot.md)) |
| F#, C#, Kotlin, Go | F# принят для Meetups ([ADR-025](../decisions/ADR-025-meetups-fsharp-stack.md)); Go принят для Identity ([ADR-027](../decisions/ADR-027-identity-go-stack.md)) |
| Scala + Apache Pekko | Future-направление для auction v2 |
| Elixir + Phoenix | Кандидат для будущего realtime gateway при появлении подтверждённых сценариев с постоянными соединениями, presence или массовым fan-out; не входит в MVP и не означает решение о замене Legacy WebSocket Gateway |
| Kotlin | Остаётся в пуле как интерес; за конкретным сервисом не закреплён |
| Go | Принят для Identity ([ADR-027](../decisions/ADR-027-identity-go-stack.md)); отдельного Scheduler-сервиса не предполагается |
| Ruby | Интересен, но отдельная админка не обоснована |
| C# + Orleans | Принят для Notifications ([ADR-029](../decisions/ADR-029-notifications-orleans-stack.md)) |

Для списка интересов ADR не нужен. ADR нужен, когда реальный сервис, требования и последствия дают выбору архитектурный вес.

## Политика runtime

По [ADR-007](../decisions/ADR-007-polyglot-service-stacks.md) язык выбирается под задачу сервиса. Общего лимита «один backend runtime по умолчанию» для MVP нет: ограничение снято владельцем. Каждый сервис выбирает runtime независимо, и появление второго или последующего runtime не требует отдельного разрешающего ADR.

Это не делает новый operational stack бесплатным. Build, deployment, observability, security updates и local development остаются обязательными критериями решения конкретного сервиса.

Текущее распределение элементов пула по сервисам, включая свободные клетки и критерии их закрытия, ведётся в [decision-matrix.html](decision-matrix.html). Матрица показывает состояние выбора и не является его источником: клетка переходит в Accepted только вместе с ADR.

## Decision experiment для backend

После продуктовых сценариев выбирается один сквозной use case, например изменение сходки организатором с последующим чтением актуального состояния. Для 2–3 кандидатов сравниваются:

- доменная модель;
- API/contract boundary;
- persistence transaction;
- обработка ошибок;
- тестируемость;
- объём инфраструктурного glue;
- время реализации владельцем.

Эксперимент не должен превращаться в несколько production-ready прототипов. Его результат — достаточно свидетельств для решения и ADR.
