name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  repo-hygiene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Скиллы синхронны между .claude и .agents
        run: sh tools/git-hooks/check-skills-mirror.sh
