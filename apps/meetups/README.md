# Meetups

Владелец данных и правил жизненного цикла сходки. Сейчас это доменное ядро среза, запись состояния и журнала одной транзакцией и связанный с ними транспорт: команды и применение событий живут в `Domain/`, шесть сценариев — в `Slices/`, доступ к PostgreSQL — в `Infrastructure/`. Все шесть операций контракта идут через свои срезы; два запроса используют общий типизированный путь чтения со смотрящим.

Сгенерированный C# не коммитится: его пишет `Grpc.Tools` в `obj/` при `dotnet build`. Рукописного кода в `Meetups.Contracts` быть не должно — это условие обратимости из [ADR-025](../../docs/decisions/ADR-025-meetups-fsharp-stack.md), и его держит `just meetups-contracts-check`, а не внимательность ревьюера.

## Устройство

```text
Meetups/
  Domain/Ids.fs                       MeetupId и PersonId
  Domain/Viewer.fs                    смотрящий: личность и общие роли
  Domain/Schedule.fs                  расписание, интервал, инвариант и ключ порядка
  Domain/MeetupSnapshot.fs            оси состояния и снимок
  Domain/Access.fs                    политика права действовать
  Domain/Meetup.fs                    состояние, события, decide, apply и rehydrate
  Migrations/001_meetups_schema.sql   схема состояния и журнала
  Migrations/002_meetup_event_dispatch.sql  отметка публикации и неизменяемость записи журнала
  Migrations/003_meetup_version_bigint.sql  версия агрегата шириной домена
  Migrations.fs                       прогон встроенных SQL-миграций через DbUp
  Infrastructure/Db.fs                источник соединений и типы дат для Dapper
  Infrastructure/MeetupRow.fs         строка таблицы и снимок, в обе стороны
  Infrastructure/MeetupEventPayload.fs  тело строки журнала
  Infrastructure/MeetupStore.fs       загрузка и запись состояния с событием одной транзакцией
  Infrastructure/MeetupReading.fs     общий путь чтения со смотрящим и областью выборки
  Slices/Contract.fs                  смотрящий, идентификатор и снимок в обе стороны
  Slices/CreateMeetupDraft.fs         четыре сценария записи: вход, отказы,
  Slices/ChangeMeetupAttributes.fs    зависимости, оркестрация, сборка зависимостей
  Slices/SetMeetupSchedule.fs         из контейнера и своя транспортная граница
  Slices/PublishMeetup.fs
  Slices/ListVisibleMeetups.fs        список через общий путь чтения
  Slices/GetMeetup.fs                 карточка через тот же путь чтения
  Observability/BoundaryLog.fs        интерцептор каркаса лога
  Transport/MeetupsGrpcService.fs     диспетчер шести операций
  Host.fs                             composition root
  Program.fs                          точка входа; применяет миграции
```

Порядок `<Compile Include>` — направление зависимостей: файл видит только объявленное выше. `Domain/` идёт первым и потому не способен увидеть транспорт, а `Infrastructure/` — выше срезов, потому что адаптеры срезов ссылаются на инфраструктурные типы, а не наоборот. Это отличается от привычной картинки Clean Architecture и следует из модели компиляции F#.

## Доменное ядро

Словарь ядра нормативен и задан [ADR-031](../../docs/decisions/ADR-031-meetups-domain-vocabulary-and-event-form.md): четыре команды, три повода события, инварианты I1–I7. Ядро детерминировано — время приходит значением, часов и генератора идентификаторов оно не читает.

Изменение поля возможно только применением события, и это свойство типа, а не дисциплина ревью: представление `Meetup` приватно, поэтому копия записи вне `Domain/Meetup.fs` не собирается, а состояние наружу читается снимком. Применение одно на все три повода, потому что состояние «до» есть и у создания: это `Initial`, «сходки ещё нет». Из него же принимаются решения, поэтому команда к несуществующей сходке — обычный отказ домена, а не предварительная проверка оболочки. Структурные инварианты закрыты так же — перевёрнутый интервал невыразим, потому что `LocalInterval` строится только смарт-конструктором.

Отказ построения значения (`ScheduleError`) и отклонённый переход состояния (`DomainError`) — разные типы: первое агрегат вообще не видит, потому что несобранное значение до него не доходит. Отображение обоих в коды gRPC закрепляет транспортный слой по таблице в [integration.md](../../docs/architecture/integration.md).

Право действовать — третий, отдельный отказ. Вариантом `DomainError` он не становится: инвариантом перехода состояния право не является, и состояние в решении о нём не участвует. Но и политикой границы он не остаётся — правило «пишущие команды доступны администратору» доменное ([ADR-031](../../docs/decisions/ADR-031-meetups-domain-vocabulary-and-event-form.md)), поэтому живёт чистой функцией в `Domain/Access.fs` со своим типом `AccessDenied`. Файл стоит до `Domain/Meetup.fs`, чтобы политика читала состояние только снимком; туда же ляжет правило видимости чтения, когда оно появится. Срез спрашивает право первой строкой `execute`, до загрузки: иначе отказ обычному смотрящему зависел бы от того, существует ли сходка, и стал бы способом узнать про чужой черновик.

`rehydrate` восстанавливает состояние из снимка, `restore` превращает отсутствие строки в `Initial`. Обе живут в `Domain/Meetup.fs` вынужденно: представление приватно, и собрать запись снаружи нельзя. Вход `rehydrate` — снимок, то есть два определения, которые [ADR-031](../../docs/decisions/ADR-031-meetups-domain-vocabulary-and-event-form.md) развёл, сегодня связаны; первое же поле состояния, которого нет в снимке, потребует своего типа восстановления.

## Запись

Состояние и доменное событие пишет одна функция — `MeetupStore.commit`, и транзакционная граница видна в её сигнатуре, а не собирается вызывающим из шагов. Конкурентная правка отклоняется предикатом `WHERE id = @id AND version = @expected_version`: ноль задетых строк и есть конфликт. Проверка стоит в предикате, а не в сравнении заранее прочитанного значения, потому что между чтением и записью строку мог изменить другой писатель, и ответить может только сама база. `UNIQUE (meetup_id, version)` в журнале остаётся второй линией и при верном предикате недостижим.

Команда, чьё решение — «события нет» (повтор создания тем же автором, повтор публикации видимой сходки), до `commit` не доходит вовсе: она возвращает уже сохранённый снимок и транзакции не открывает. Поэтому `commit` принимает событие значением, а не `option` — по типу видно, что успешный возврат означает записанную пару строк.

Конверт события заполняет оболочка: `event_id` рождается до транзакции и дальше неизменяем, `occurred_at` — тот же момент, из которого принято решение. Тело строки журнала — снимок в JSON, форма внутренняя и закреплена тестом: конверт публикации наружу остаётся открытой контрактной задачей, а `dispatched_at` эта запись не трогает — отметку ставит релей.

Dapper не знает `DateOnly` и `TimeOnly` как параметры, хотя Npgsql пишет их в `date` и `time` нативно. Разрыв закрывают два type handler в `Infrastructure/Db.fs`; сопоставление колонок с полями записи при этом задано алиасами в самих запросах, а не глобальным `MatchNamesWithUnderscores`.

## Чтение

`ListVisibleMeetups` и `GetMeetup` — два самостоятельных query slice, но строку `meetups` читают только через `MeetupReading.read`. Путь требует `Viewer` и закрытый DU `Scope = All | ById`, поэтому новый публичный способ обойти смотрящего или собрать произвольный SQL не появляется. `GetMeetup` отображает отсутствие строки в `NOT_FOUND`; список возвращает снимки в порядке расписания: ранняя дата раньше поздней, в один день дата без времени раньше времени, `no_date` в конце. Равные расписания намеренно не получают скрытого порядка по id или заголовку.

PER-58 закрепляет форму единого пути, но ещё не реализует правило видимости ADR-022: до PER-59 аргумент `Viewer` проходит типизированный путь, а выборка временно возвращает все сохранённые строки. Поэтому эти RPC пока нельзя считать готовой границей конфиденциальности. В PER-59 фильтр добавляется внутри `MeetupReading`, а не в отдельных срезах; форма двух запросов и диспетчер при этом не меняются.

Класс `MeetupsGrpcService` — диспетчер, а не место логики. ASP.NET Core принимает реализацию сервиса одним классом со всеми операциями сразу, поэтому отдельного `Api.fs` на срез для gRPC не бывает; как это раскладывается, описывает [норматив](../../docs/standards/architecture/functional-slices.md#grpc). Метод диспетчера собирает зависимости своего среза и отдаёт их его же модулю `Api`; `ServerCallContext` глубже не проходит — из него берётся только `RequestServices`. Ветвление по содержимому запроса в этом файле означает, что граница поехала.

Отображение отказа в код объявляет срез, а не диспетчер: общий `mapError` на сервис заставил бы каждую операцию разбирать чужие отказы. Коды берутся из таблицы в [integration.md](../../docs/architecture/integration.md): `INVALID_ARGUMENT` на дефект сборки запроса, `PERMISSION_DENIED` на отсутствие права, `FAILED_PRECONDITION` на нарушенный инвариант, `NOT_FOUND` на несуществующую и на чужую сходку одинаково. Конфликт версии отвечает `ABORTED` — реализационный выбор, контрактную строку закрепляет [PER-78](https://linear.app/anticnvm/issue/per-78). Пара «срез и невозможный для него вариант `DomainError`» отображается не кодом, а `invalidOp`: это нарушение внутреннего контракта, а не отказ, который клиент должен читать.

`Host.build` вынесен из `Program.fs` отдельной функцией, чтобы интеграционный тест поднимал ровно тот же хост, что и запуск. Миграции применяет `Program`, а не `Host.build`: gRPC-тесты каркаса по-прежнему не требуют базу.

## Транспорт и готовность

Kestrel настроен на h2c: gRPC без TLS требует HTTP/2, а plaintext-endpoint без ALPN не умеет договариваться о версии. Отсюда два следствия:

- `MapDefaultEndpoints` из ServiceDefaults не вызывается — `/health` и `/alive` на таком endpoint недостижимы;
- готовность сервис отдаёт по `grpc.health.v1`. Источником состояния остаётся реестр health checks из ServiceDefaults, gRPC-сервис — только его витрина. Ту же пробу использует Aspire.

Reflection включена безусловно, как в Identity: без неё каждая ручная проверка `grpcurl` требует `-import-path` и `-proto`.

## Команды

Из корня репозитория:

```bash
just meetups-build
just meetups-test
just meetups-contracts-check
just meetups-run
```

`MEETUPS_DATABASE_URL` обязателен для `meetups-run`: процесс применяет миграции DbUp до прослушивания. Интеграционные тесты схемы поднимают PostgreSQL через Testcontainers; если демона нет, берут `MEETUPS_DATABASE_URL` или `127.0.0.1:5432`, а без доступной базы пропускаются. В CI контейнер обязателен.

Команда генерации — `dotnet build` контрактного проекта `apps/meetups/Meetups.Contracts`.

Тесты идут на xUnit v3 с Unquote и запускаются через Microsoft.Testing.Platform: runner выбран ключом `test` в корневом `global.json`, решение передаётся флагом `--solution`. Подробности и грабли — в [руководстве по локальной разработке](../../docs/development/local-development.md).

`Meetups.UnitTests` проверяет доменное ядро, политику права, порядок расписаний, разбор и отрисовку контракта, коды отказов операций, срезы чтения, публичную форму общего reader API, ветки отказа границы и состав встроенных миграций без хоста. Тесты ядра лежат в `DomainTests/`, тесты срезов и границы — в `SliceTests/`, тест формы инфраструктурного API — в `InfrastructureTests/`, общий валидный образец — в `TestUtilities/TestData/Sample.fs`; PostgreSQL и контейнеры им не нужны. Коды отказов проверяются здесь, а не через провод, потому что конфликт версии снаружи не воспроизводится: `expected_version` во вход команд не входит. `Meetups.IntegrationTests` поднимает настоящий Kestrel на свободном порту, ходит в него настоящим gRPC-каналом и проверяет операции контракта, `grpc.health.v1` и каркас лога границы; отдельно применяет миграции на изолированной базе и проверяет ограничения схемы. Фикстуры лежат в `Infrastructure/`, сценарии — в `Scenarios/`. Команды записи проверяются там же против настоящего PostgreSQL: атомарность пары «состояние и событие» читается по совпадению `xmin` обеих строк, отклонение по версии — двумя командами, принятыми из одной версии, идемпотентность — повтором создания и публикации. Читающие сценарии проверяют пустой список, сохранённый снимок, `NOT_FOUND`, порядок и запись каркаса лога. Эти свойства пути через базу проверить без неё нельзя; на машине без Docker и без `MEETUPS_DATABASE_URL` они пропускаются, а прогон остаётся зелёным. Записывающий логгер и фейковый `ServerCallContext` нужны обоим проектам и живут в `Meetups.TestKit`.

Фикстур хоста две, и разделены они по признаку «нужна ли база». `MeetupsHostFixture` поднимает хост с синтаксически валидным и заведомо недостижимым DSN: `NpgsqlDataSource.Create` соединение не открывает, поэтому хост живёт, а упадёт только вызов, который действительно пойдёт в базу. На ней проверяются каркас границы и те отказы, что принимаются до хранилища, — по праву и по разбору запроса; зелёный тест там доказывает заодно, что до базы вызов не дошёл. Пустая переменная вместо недостижимого адреса не годится: `Host.build` на ней делает `failwith` уже при разрешении источника. `LiveMeetupsHost` поднимает тот же хост с настоящей изолированной базой и проверяет сквозной путь команд и запросов. Она создаётся в теле теста, а не `IClassFixture`, и это не стиль: `IsolatedDatabase` зовёт `Assert.Skip`, а skip из конструктора class fixture xUnit показывает падением класса, и прогон без Docker покраснел бы.

## Ручная проверка

Профиль `meetups` поднимает сервис вместе с PostgreSQL; Identity в него не входит:

```bash
just aspire meetups
```

Порт назначает Aspire, поэтому адрес читается из `aspire describe`, а не берётся фиксированным:

```bash
grpcurl -plaintext localhost:<порт> list
```

Автотесты покрывают сам сервис; руками проверяется то, что Aspire собрал его работающим — привязка `MEETUPS_DATABASE_URL`, endpoint `grpc` и reflection. Минимальный сценарий: `list` показывает `meetups.v1.MeetupsService`; `CreateMeetupDraft` со смотрящим, у которого `"global_roles": ["GLOBAL_ROLE_ADMIN"]`, отвечает снимком с `version: 1`; после заполнения атрибутов и `PublishMeetup` оба запроса чтения возвращают тот же снимок:

```bash
grpcurl -plaintext -d '{"viewer":{"identityId":"0199c0de-0000-7000-8000-00000000000a"}}' localhost:<порт> meetups.v1.MeetupsService/ListVisibleMeetups
grpcurl -plaintext -d '{"viewer":{"identityId":"0199c0de-0000-7000-8000-00000000000a"},"id":"0199c0de-0000-7000-8000-000000000001"}' localhost:<порт> meetups.v1.MeetupsService/GetMeetup
```

Карточка по отсутствующему `id` отвечает `Code: NotFound`, а запрос без `viewer` — `Code: InvalidArgument`. Идентификатор в запросе — каноническая строка UUIDv7 в нижнем регистре с дефисами, иначе граница ответит `InvalidArgument`. До PER-59 список и карточка временно не скрывают сохранённые строки по `visibility`; это известное ограничение среза, а не ожидаемое поведение готового продукта.
