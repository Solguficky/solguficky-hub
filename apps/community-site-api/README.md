# Community site API

Функции сайта сообщества. Сейчас одна: `/api/notes` хранит то, что админы заполняют на странице [«Аукцион 2026»](https://solguficky.netlify.app/auction-2026) — ответы на вопросы, оценки фич и список приоритетов.

До этого заполненное жило в `localStorage` и умирало вместе с браузером: перенести его на другое устройство или показать другому админу было нечем, а ошибочную правку нельзя было отменить. Теперь заполненное живёт одним документом на сервере, у документа есть история зафиксированных версий, и к любой из них можно вернуться.

Логина нет: пропуском служит идентификатор документа в ссылке. Кому передали ссылку, тот и редактирует.

## Команды

Из корня репозитория:

```bash
just community-site-api-tools
just community-site-api-typecheck
just community-site-api-lint
just community-site-api-test
```

Сборки нет: функцию бандлит Netlify CLI при деплое сайта, тем же прогоном и тем же токеном, что и статику. Доставка описана в [ci.md](../../docs/development/ci.md).

## Устройство

`src/document.ts` — вся логика документа: разбор недоверенного состояния, ревизии, версии, откаты. Модуль чистый, поэтому история и откаты проверяются обычными тестами, без Netlify и без сети.

`netlify/functions/notes.mts` — HTTP-граница: маршрут, коды ответа, Netlify Blobs.

Контракт, границы и причины решений — в [AGENTS.md](AGENTS.md).

## Прочитать заметки снаружи

```bash
curl -s https://solguficky.netlify.app/api/notes/<идентификатор-документа>
```

Ответ несёт текущее состояние и все версии. Идентификатор — тот же, что в ссылке на странице: кнопка «Скопировать ссылку» отдаёт её целиком.
